function findPlayersWithZeroOrOneLosses(matches) {
  //   let winners = matches.map((match) => match[0]);
  let ans = [];
  const lostCounts = new Map();
  for (let match of lostCounts) {
    lostCounts.set(match[1], (lostCount.get(match[1]) || 0) + 1);
  }
}
